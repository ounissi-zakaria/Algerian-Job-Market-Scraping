# Automatically created by: scrapyd-deploy

from setuptools import setup, find_packages

setup(
    name='Algerian-Job-Market',
    version='1.0',
    packages=find_packages(),
    entry_points={'scrapy': ['settings = Algerian_Job_Market.settings']},
)
